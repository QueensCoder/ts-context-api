// import React, { createContext, useReducer } from "react";
// import reducer, { increment, decrement } from "../reducer/counter";

// export const CountContext = createContext<CounterContext>({
//   count: 0,
//   updateCount: n => console.log("hello world")
// });

// const CountContextProvider: CountContextProvider = ({ children }) => {
//   return <CountContext.Provider>{children}</CountContext.Provider>;
// };

// export default CounterContextProvider;
export {};
